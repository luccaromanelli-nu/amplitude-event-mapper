project_name: demo-ppf-mx
scope: |
  PPF widget awareness optimization on Cash Flow Financing hub (synthetic demo).
  Test exposure-vs-engagement on three cells: control, soft, hard.
  Used as the bundled demo for the amplitude-event-mapper plugin — fixtures only,
  no real Figma / Miro / Databricks calls.

country: MX
ab_test: true
expr_id: ppf-awareness-mx
cells: [control, soft, hard]

sources:
  figma:
    - fixtures/figma.json
  miro:
    - fixtures/miro.json
  confluence:
    - fixtures/confluence.md

squad: cc-financing
owner: cc-financing

default_provider: finn
default_package: catalyst_entrypoint
default_entrypoint_type: bdc-widget
